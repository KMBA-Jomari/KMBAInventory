<%= form_with(model: subcategory) do |form| %>
  <% if subcategory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
        <% subcategory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :sub_category_name, style: "display: block" %>
    <%= form.text_field :sub_category_name %>
  </div>

  <div>
    <%= form.label :description_string, style: "display: block" %>
    <%= form.text_field :description_string %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
